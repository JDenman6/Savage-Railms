<%= form_for(character_skill) do |f| %>
  <% if character_skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(character_skill.errors.count, "error") %> prohibited this character_skill from being saved:</h2>

      <ul>
      <% character_skill.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :linked_attribute %>
    <%= f.collection_select :character_attribute_id, CharacterAttribute.all,
    :id, :name, :prompt => "Please select an attribute" %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
